# pnpm v11 构建审批白名单（替代旧版 onlyBuiltDependencies）
# 这些依赖需要运行安装脚本（bcrypt 需编译原生模块，unrs-resolver 需下载平台二进制）
allowBuilds:
  bcrypt: true
  unrs-resolver: true
  # @scarf/scarf 是某依赖携带的遥测包，构建脚本非必需，显式忽略以消除 CI 报错
  '@scarf/scarf': false
